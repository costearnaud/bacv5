<form method="post" action="{{ path('team_veteran_delete', {'id': team_veteran.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_veteran.id) }}">
    <button class="btn">Delete</button>
</form>
