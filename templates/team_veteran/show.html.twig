{% extends 'base.html.twig' %}

{% block title %}TeamVeteran{% endblock %}

{% block body %}
    <h1>TeamVeteran</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team_veteran.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ team_veteran.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ team_veteran.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('team_veteran_index') }}">back to list</a>

    <a href="{{ path('team_veteran_edit', {'id': team_veteran.id}) }}">edit</a>

    {{ include('team_veteran/_delete_form.html.twig') }}
{% endblock %}
