{% extends 'base.html.twig' %}

{% block title %}Interclub{% endblock %}

{% block body %}
    <h1>Interclub</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interclub.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ interclub.name }}</td>
            </tr>
            <tr>
                <th>DateRencontre</th>
                <td>{{ interclub.dateRencontre ? interclub.dateRencontre|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ interclub.score }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ interclub.location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('interclub_index') }}">back to list</a>

    <a href="{{ path('interclub_edit', {'id': interclub.id}) }}">edit</a>

    {{ include('interclub/_delete_form.html.twig') }}
{% endblock %}
