<form method="post" action="{{ path('interclub_delete', {'id': interclub.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interclub.id) }}">
    <button class="btn">Delete</button>
</form>
