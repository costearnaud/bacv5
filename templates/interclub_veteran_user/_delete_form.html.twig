<form method="post" action="{{ path('interclub_veteran_user_delete', {'id': interclub_veteran_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interclub_veteran_user.id) }}">
    <button class="btn">Delete</button>
</form>
