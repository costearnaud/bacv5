{% extends 'base.html.twig' %}

{% block title %}InterclubVeteranUser{% endblock %}

{% block body %}
    <h1>InterclubVeteranUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interclub_veteran_user.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ interclub_veteran_user.type }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ interclub_veteran_user.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('interclub_veteran_user_index') }}">back to list</a>

    <a href="{{ path('interclub_veteran_user_edit', {'id': interclub_veteran_user.id}) }}">edit</a>

    {{ include('interclub_veteran_user/_delete_form.html.twig') }}
{% endblock %}
