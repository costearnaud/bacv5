<form method="post" action="{{ path('lien_doodle_delete', {'id': lien_doodle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lien_doodle.id) }}">
    <button class="btn">Delete</button>
</form>
