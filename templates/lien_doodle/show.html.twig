{% extends 'base.html.twig' %}

{% block title %}LienDoodle{% endblock %}

{% block body %}
    <h1>LienDoodle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lien_doodle.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ lien_doodle.value }}</td>
            </tr>
            <tr>
                <th>UpdatedDt</th>
                <td>{{ lien_doodle.updatedDt ? lien_doodle.updatedDt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lien_doodle_index') }}">back to list</a>

    <a href="{{ path('lien_doodle_edit', {'id': lien_doodle.id}) }}">edit</a>

    {{ include('lien_doodle/_delete_form.html.twig') }}
{% endblock %}
