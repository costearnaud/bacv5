{% extends 'base.html.twig' %}

{% block title %}InterclubVeteran{% endblock %}

{% block body %}
    <h1>InterclubVeteran</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ interclub_veteran.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ interclub_veteran.name }}</td>
            </tr>
            <tr>
                <th>DateRencontre</th>
                <td>{{ interclub_veteran.dateRencontre ? interclub_veteran.dateRencontre|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ interclub_veteran.score }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ interclub_veteran.location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('interclub_veteran_index') }}">back to list</a>

    <a href="{{ path('interclub_veteran_edit', {'id': interclub_veteran.id}) }}">edit</a>

    {{ include('interclub_veteran/_delete_form.html.twig') }}
{% endblock %}
