{% extends 'base.html.twig' %}

{% block title %}InterclubVeteran index{% endblock %}

{% block body %}
    <h1>InterclubVeteran index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>DateRencontre</th>
                <th>Score</th>
                <th>Location</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for interclub_veteran in interclub_veterans %}
            <tr>
                <td>{{ interclub_veteran.id }}</td>
                <td>{{ interclub_veteran.name }}</td>
                <td>{{ interclub_veteran.dateRencontre ? interclub_veteran.dateRencontre|date('Y-m-d') : '' }}</td>
                <td>{{ interclub_veteran.score }}</td>
                <td>{{ interclub_veteran.location }}</td>
                <td>
                    <a href="{{ path('interclub_veteran_show', {'id': interclub_veteran.id}) }}">show</a>
                    <a href="{{ path('interclub_veteran_edit', {'id': interclub_veteran.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('interclub_veteran_new') }}">Create new</a>
{% endblock %}
