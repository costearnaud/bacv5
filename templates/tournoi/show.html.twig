{% extends 'base.html.twig' %}

{% block title %}Tournoi{% endblock %}

{% block body %}
    <h1>Tournoi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournoi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tournoi.name }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ tournoi.dateDebut ? tournoi.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ tournoi.dateFin ? tournoi.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ tournoi.lieu }}</td>
            </tr>
            <tr>
                <th>LienInscription</th>
                <td>{{ tournoi.lienInscription }}</td>
            </tr>
            <tr>
                <th>Classements</th>
                <td>{{ tournoi.classements }}</td>
            </tr>
            <tr>
                <th>Categories</th>
                <td>{{ tournoi.categories }}</td>
            </tr>
            <tr>
                <th>Tableaux</th>
                <td>{{ tournoi.tableaux }}</td>
            </tr>
            <tr>
                <th>DateInscription</th>
                <td>{{ tournoi.dateInscription ? tournoi.dateInscription|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>LienConvocation</th>
                <td>{{ tournoi.lienConvocation }}</td>
            </tr>
            <tr>
                <th>LienDescription</th>
                <td>{{ tournoi.lienDescription }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ tournoi.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournoi_index') }}">back to list</a>

    <a href="{{ path('tournoi_edit', {'id': tournoi.id}) }}">edit</a>

    {{ include('tournoi/_delete_form.html.twig') }}
{% endblock %}
