<form method="post" action="{{ path('doodle_delete', {'id': doodle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doodle.id) }}">
    <button class="btn">Delete</button>
</form>
