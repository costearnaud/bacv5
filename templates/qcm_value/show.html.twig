{% extends 'base.html.twig' %}

{% block title %}QcmValue{% endblock %}

{% block body %}
    <h1>QcmValue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ qcm_value.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ qcm_value.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ qcm_value.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('qcm_value_index') }}">back to list</a>

    <a href="{{ path('qcm_value_edit', {'id': qcm_value.id}) }}">edit</a>

    {{ include('qcm_value/_delete_form.html.twig') }}
{% endblock %}
