<form method="post" action="{{ path('age_category_delete', {'id': age_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ age_category.id) }}">
    <button class="btn">Delete</button>
</form>
