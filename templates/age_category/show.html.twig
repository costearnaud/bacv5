{% extends 'base.html.twig' %}

{% block title %}AgeCategory{% endblock %}

{% block body %}
    <h1>AgeCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ age_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ age_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('age_category_index') }}">back to list</a>

    <a href="{{ path('age_category_edit', {'id': age_category.id}) }}">edit</a>

    {{ include('age_category/_delete_form.html.twig') }}
{% endblock %}
